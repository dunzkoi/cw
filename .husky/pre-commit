#!/usr/bin/env sh
# cw pre-commit: 전체 테스트 스위트 실행 (회귀 방지)
# 우회 필요 시: git commit --no-verify

echo "▶ check.sh 실행 중..."
( cd "$(git rev-parse --show-toplevel)" && ./check.sh ) || {
  echo ""
  echo "✗ check.sh 실패 — 커밋 중단."
  exit 1
}
